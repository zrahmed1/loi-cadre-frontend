Place exported report files here. Name format: loi-cadre-<id>.xlsx
Example: loi-cadre-1.xlsx

If a file matching the clicked loi-cadre id exists here, the app will download it; otherwise it will call the backend export endpoint.
